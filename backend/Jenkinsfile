pipeline {
    agent any

    stages {
        // stage('Build') {
        //     steps {
        //         sh '''
        //             cd backend
        //             chmod +x gradlew
        //             ./gradlew clean install
        //         '''
        //     }
        // }

        stage('Test') {
            steps {
                sh '''
                    cd backend
                    chmod +x gradlew
                    ./gradlew clean test
                '''
            }
        }
    }
}
