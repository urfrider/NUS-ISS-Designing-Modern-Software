pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                    cd backend
                    ./gradlew clean install
                '''
            }
        }

        stage('Test') {
            steps {
                sh '''
                    cd backend
                    ./gradlew clean test
                '''
            }
        }
    }
}
